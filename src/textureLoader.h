#include "texture.h"

// fill texture array with noisy version of scenename
void load_8povTexture(Texture *textureArray[8],std::string ImageDatabase_Path,int noise)
{
    for(int i=0;i<8;i++)
    {
        //textureArray[i]->load(ImageDatabase_Path+"/p3d_crown-0"+std::to_string(i+1)+"_00"+"100.png");
    }
    return;
}